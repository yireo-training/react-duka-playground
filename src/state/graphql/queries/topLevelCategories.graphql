query topLevelCategories @cache(id: "top-level-categories", timeout: 3600) {
  category(id: 2) {
    children {
      url_path
      id
      name
      children {
        url_path
        id
        name
      }
    }
  }
}
